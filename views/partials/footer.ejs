      <footer>
        <p class="pull-right"><a href="#">Back to top</a></p>
        <p>&copy; 2017 Todo App, Inc. &middot; <a href="#">Privacy</a> &middot; <a href="#">Terms</a></p>
      </footer>


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    <script src="https://cdn.socket.io/socket.io-1.2.0.js"></script>
    <script>
        $(document).ready(function(){
            var socket = io();
            
            $('#chatForm').submit(function(){
                socket.emit('chat message', {
                    message: $('#message').val(),
                    user: $('#user').val()
                });

                $('#message').val('');
                return false;
           });

            socket.on('chat message', function(msg){
                var html = '<div class="row message-bubble">';
                    html += '<p class="text-muted">'+ msg.user +' '+ msg.created +'</p>';
                    html += '<span>'+ msg.content +'</span>';
                    html += '</div>';
                $('#messages').append(html);
                $("html, body").scrollTop($(document).height());
            });

            socket.on('output', function(messages, err){
                if(messages.length) {
                    for(var i = 0; i < messages.length; i++){
                        var html = '<div class="row message-bubble">';
                        html += '<p class="text-muted">'+ messages[i]._user.nick +' '+ messages[i].created +'</p>';
                        html += '<span>'+ messages[i].content +'</span>';
                        html += '</div>';
                        $('#messages').append(html);
                    }
                }
            });
            
            socket.on('er', function(er){
                alert(er);
            });

        });
    </script>
</body>
</html>